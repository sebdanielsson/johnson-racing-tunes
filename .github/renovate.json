{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "semanticCommits": "enabled",
  "minimumReleaseAge": "1 day",
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch"],
      "matchCurrentVersion": "!/^0/",
      "automerge": true,
      "matchPackageNames": ["!typescript"]
    }
  ],
  "customManagers": [
    {
      "description": "Update the pnpm version in devEngines.packageManager, and the matching packageManagerDependencies entry in pnpm-lock.yaml (pnpm install --frozen-lockfile fails if they disagree). Renovate's npm manager only updates the top-level packageManager field, which these repositories no longer set. Both files share the pnpm dependency name, so their updates land in one branch.",
      "customType": "regex",
      "managerFilePatterns": ["/(^|/)package\\.json$/", "/(^|/)pnpm-lock\\.yaml$/"],
      "matchStrings": [
        "\"packageManager\":\\s*\\{\\s*\"name\":\\s*\"pnpm\",\\s*\"version\":\\s*\"(?<currentValue>[^\"]+)\"",
        "packageManagerDependencies:\\n\\s+pnpm:\\n\\s+specifier: (?<currentValue>\\S+)",
        "packageManagerDependencies:\\n\\s+pnpm:\\n\\s+specifier: \\S+\\n\\s+version: (?<currentValue>\\S+)"
      ],
      "depNameTemplate": "pnpm",
      "datasourceTemplate": "npm"
    }
  ]
}
